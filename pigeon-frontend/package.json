{
  "name": "pigeon-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "animejs": "^3.0.1",
    "apollo-boost": "^0.4.1",
    "apollo-cache-inmemory": "^1.6.2",
    "apollo-link": "^1.2.11",
    "apollo-link-context": "^1.0.17",
    "apollo-link-http": "^1.5.14",
    "axios": "^0.19.0",
    "graphql": "^14.3.1",
    "node-sass": "^4.12.0",
    "react": "^16.8.6",
    "react-apollo": "^2.5.6",
    "react-apollo-hooks": "^0.4.5",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1",
    "react-transition-group": "^4.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "npm install && rm -rf ../public && mkdir ../public && react-scripts build && mv build/* ../public",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "server": "json-server -p3001 database.json"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "eslint-config-prettier": "^4.3.0",
    "eslint-plugin-prettier": "^3.1.0",
    "json-server": "^0.15.0",
    "prettier": "^1.17.1"
  }
}
